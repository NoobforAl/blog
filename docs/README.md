# Documentation

- [**deployment.md**](./deployment.md) — building `dist/`, static hosting
  (cPanel/Apache), Docker behind a reverse proxy (HAProxy), and the CI /
  tag-based release pipeline.
- [**environment.md**](./environment.md) — every environment variable, split
  into build-time (baked into the static files) and runtime (`static-web-server`).
